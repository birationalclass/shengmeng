import {proofPanel} from './proof-panel.js?v=153';
export function spectralExampleExposition({math,language}){
 const R=String.raw,t=(z,e)=>language()==='en'?e:z,M=x=>math(x),eq=x=>`<div class="operation-equation">${math(x,true)}</div>`;
 const map=R`\begin{array}{rcl}\Phi_0^{p,q}:K^{p,q}&\xrightarrow{\sim}&E_0^{p,q}\\a&\longmapsto&[a]_0=a+F^{p+1}C^{p+q}\end{array}`;
 const compatibility=R`d_0^{p,q}\Phi_0^{p,q}=\Phi_0^{p,q+1}\delta_2^{p,q}`;
 const details=`<p><strong>${t('例。','Example.')}</strong> ${t('设双复形及列滤过如第一节。则有如下自然同构，且它与纵向微分相容：','Let the double complex and its column filtration be as in Section 1. Then the following natural isomorphism is compatible with the vertical differential:')}</p>${eq(map)}${eq(compatibility)}<p><strong>${t('证明。','Proof.')}</strong> ${t('取','Put')} ${M('n=p+q')}。${t('由滤过保持性，','Since D preserves the filtration,')}</p>${eq(R`Z_0^{p,q}=F^pC^n,\quad Z_{-1}^{p+1,q-1}=F^{p+1}C^n,\quad B_{-1}^{p,q}\subseteq F^{p+1}C^n`)}${eq(R`E_0^{p,q}=F^pC^n/F^{p+1}C^n,\qquad F^pC^n=K^{p,q}\oplus F^{p+1}C^n`)}<p>${t('因此每个陪集有唯一的第 p 列代表元，逆映射取第 p 列分量。对','Thus every coset has a unique representative in column p; the inverse takes this component. For')} ${M(R`a\in K^{p,q}`)}，</p>${eq(R`Da=\delta_1a+\delta_2a,\qquad\delta_1a\in F^{p+1}C^{n+1},\qquad [Da]_0=[\delta_2a]_0`)}<p>${t('这给出所需的微分相容性。','This proves compatibility with the differentials.')}</p><section class="proof-detail-section"><h3>${t('练习：第一页','Exercise: The first page')}</h3><p>${t('利用 2.3 和上述同构，证明','Using 2.3 and the preceding isomorphism, prove that')}</p>${eq(R`E_1^{p,q}\cong H^q(K^{p,\bullet},\delta_2)=\frac{\ker\delta_2^{p,q}}{\operatorname{im}\delta_2^{p,q-1}}`)}<p>${t('对纵向闭元 a，记其上同调类为 [a]₁；验证下式与代表元选择无关：','For a vertical cocycle a, denote its cohomology class by [a]₁. Verify that the following map is independent of the representative:')}</p>${eq(R`d_1^{p,q}:E_1^{p,q}\longrightarrow E_1^{p+1,q},\qquad[a]_1\longmapsto[\delta_1^{p,q}a]_1`)}<p>${t('提示：使用反交换关系分别检验闭元与边界。','Hint: use anticommutation to check cocycles and boundaries separately.')}</p></section>`;
 return proofPanel({key:'e0-example',title:t('2.6 例：第零页','2.6 Example: Zeroth page'),formulas:[map,compatibility],note:t('同构的逆映射取第 p 列分量。横向分量在商中消失，因此 d₀ 对应 δ₂。','The inverse takes the column-p component. The horizontal component vanishes in the quotient, so d₀ corresponds to δ₂.'),details,detailsAreComplete:true,math,language});
}
export function spectralFamilyExposition({math,language}){
 const t=(z,e)=>language()==='en'?e:z;
 return `<p class="operation-note">${t(`谱序列由各个双分次页 ${math('(E_r^{p,q},d_r^{p,q})')} 和指定的结构同构 ${math('E_{r+1}^{p,q}\\cong H^{p,q}(E_r,d_r)')} 组成。这里微分的双次数为 ${math('(r,1-r)')}。`,`A spectral sequence consists of bigraded pages ${math('(E_r^{p,q},d_r^{p,q})')} together with specified structure isomorphisms ${math('E_{r+1}^{p,q}\\cong H^{p,q}(E_r,d_r)')}. The differentials have bidegree ${math('(r,1-r)')}.`)}</p><p class="operation-note">${t(`前一页的上同调确定下一页的空间；下一页的微分并不由此前的页和微分单独确定。在本文的构造中，它由原滤过复形的 ${math('D')} 诱导。`,`Cohomology determines the next page as a space; its new differential is not determined by the preceding page and differential alone. In our construction it is induced by ${math('D')} in the original filtered complex.`)}</p>`;
}
