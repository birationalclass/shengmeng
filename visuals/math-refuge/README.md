# 数学难民营 — A retreat for wandering minds

An original, navigable 3D architectural scene for Sheng Meng's Visual Lab. **Not a video, image panorama, or 2.5D photo displacement.** The structure, rooms, furniture, plants, sea-access steps and distant islands are modeled geometry. This is an initial reference-inspired scene, not a verified pixel-equivalent reconstruction of the supplied film.

## Reference and camera design

Reference: [梦境之初q — 刷到就说明你未来将会拥有这样的房子](https://www.bilibili.com/video/BV1umun6PEvs/), 8:09, posted 2026-08-06, labelled as including AI-generated content by Bilibili. Initial access returned 412; a later ordinary browser opening succeeded. Observed opening material includes a layered dark-framed glass hillside villa, terraces, a reflecting-water passage with stone steps, warm linear lighting, an atrium and window-side interiors. We have **not** extracted the original camera tracks, underlying models or material maps, nor verified every shot of the full film. No original frames, audio or models are redistributed.

The eight-chapter tour uses arc-length sampled Catmull–Rom camera and target curves with eased movement: coastal arrival, courtyard dolly, library, auditorium, a dedicated chalkboard lesson, ocean terrace, rising retreat and a contained offshore garden chapter. The auditorium camera follows the central aisle rather than crossing seats. The lesson camera follows the active board pair with a damped, level close view. Each chapter has its own position, target and field of view. Every chapter transition continuously interpolates camera position, target and field of view; manual dragging stops the tour and takes over the current view. No chapter uses a blackout or instantaneous jump. Free-view resume interpolation has no architectural collision avoidance. These are inspired camera categories, **not motion capture from the reference**.

## Expandable offshore campus — 2026-09-20

This revision follows the **fixed two-storey hall, arch bridges, north coffee cabin, shorter terrace and blue daytime sky** requests. Pools remain removed; seawater separates the connected platforms. It is a layout/model revision, not a completed architectural engineering design.

- Cardinal directions are explicit: east is +X, north is −Z. All buildings, service paths, gardens and expansion docks sit on modeled offshore platforms. All near-campus mountain, mainland and natural waterfall/creek geometry is removed. No support piles are generated. Decks stand about 1.3 m above mean sea level with continuous fascia extending underwater; planting survives only in the contained offshore garden trays and tree wells. Four islands occupy distant oblique views; the central due-east view has no islands or boats. Sunrise is east; the simulated sun moves west during the day.
- Two low, transparent main-villa wings flank the dry courtyard. They contain an academic living room and discussion room, with smaller upper studies/seminar rooms and terraces. A detached quiet library, two residential villas and a service/tea-kitchen pavilion sit farther west. One small timber tea pavilion provides a classical accent.
- The swimming pool remains removed. The east terrace is shortened to plan X=24–54, Z=−16.5–16.5 (990 plan-square units, down from 3,360); chaise lounges are removed. Two small sofa groups remain near its edges, leaving the east view open. Three shallow timber arch bridges cross genuine seawater gaps between the main wings, hall platform and new north coffee cabin. The cabin has a counter, modeled two-group espresso machine with cups and bean hoppers, seating and its own functional sign.
- Twelve broad seven-step stairways link the principal ground-floor terraces to the sea, with about 18 cm risers and 54 cm treads. Stair feet end 3.5 cm above mean sea level. The new bridges have slender side rails. This virtual layout is not a certified coastal or accessibility construction design.
- The **30-seat offshore seminar hall** restores its 20-plan-unit transverse width while increasing clear height to 4.9 m. Each of the three rows has ten seats, in two five-seat blocks; within-block seat centers are 1.2 plan units apart (previously 1.5). The front row moves back by 1 plan unit (about 1.41 m), leaving 4.95 m from its center to the writing plane. The three rows have physical rises of 0, 0.18 and 0.36 m from front to back, two seat blocks, a central carpeted stair aisle with 9 cm steps and a small lectern. Deep warm-gray curved wraparound shells, reclined smoke-brown upholstery, broad soft seat cushions, headrests, champagne supports and walnut writing tablets form a quieter lounge palette. Headrests share the same physical support curve as the upholstery, with a 5 mm separation and no sideways roll. The carpet covers both floor and stepped tiers, deepening from light taupe in front through medium taupe to darker taupe at the back; the north opaque acoustic wall is removed to preserve the left-hand sea view. Listeners face east; the six blackboards stand at the eastern teaching front and face west. Their linear dimensions grow by 20% (lecture scale 0.60 to 0.72); the lower writing edge is now 1.356 m above the front floor. Both upper boards and the backing/rails clear the raised roof. Physical control buttons remain at reachable heights rather than moving up with the writing surface. A separate operable blackout shade controls glare behind the boards. The structure is on supported platforms above actual seawater, not filled land.
- Site coordinates retain the existing √2 scale, while room clear heights and furniture use physical dimensions. The legacy board assembly is independently scaled and rotated into the compact hall. All board-focus cameras use its transformed normal. The blackboard bottom is at least 0.905 m above the floor at every lift position. The original notes, six-board alternation, chalk wear/dust and damp wiping remain intact.
- Paths, roofs, glazing gaps, stairs, furniture and books are modeled geometry. Room functions are blocked out; detailed services, engineering and every interior fixture are not complete. Free observation has no wall-collision walking physics and can pass through rooms. A shared water-height guard runs after orbit/dolly/pan, keyboard movement and automatic cameras: the eye stays at least 1 m above sea level, and the orbit target cannot sink below that surface. Looking up toward the roof is still allowed.

The enlarged hall terrace uses large-format, honed medium-dark gray-brown stone slabs with subtle generated mineral clouding, low-contrast 6 mm joints, a charcoal border and a single matte champagne entry threshold. It replaces the generic small-square tile grid. All intersecting architectural platforms are tessellated as nonoverlapping unions rather than stacking coplanar floors; generic fine grid lines and repeated platform-edge lights are removed. Camera near clipping increases from 0.08 to 0.2 m and shadow bias is adjusted. These are code-level anti-flicker measures; visual acceptance remains unperformed.

### Fixed two-storey hall, scene clock and planting

The retractable roof and its controls are removed. A fixed upper lounge with terrace and a 34-step external west staircase follows the main villa's two-storey language. Ground-floor clear height remains 4.9 m, preserving the raised boards and seats. East glazing beside the boards now uses four wide panes with three narrow joints. The scene follows device local time; a settings slider provides a temporary time preview. Room-specific warm fixtures remain; the hall now has a fixed matte acoustic ceiling with three linear diffusers. Twenty-one actual light sources serve the campus, including upper rooms, reading areas and dedicated blackboard lighting. No extra shadow maps or bloom are introduced. Environment refresh remains throttled to half-hour steps. Clear daytime sky uses turbidity 1.8, Rayleigh 2 and Mie coefficient 0.002; blue distance fog has density 0.00028 at noon, increasing slightly at twilight/night. This is a visual daylight model, not calibrated astronomy or lighting.

A transparent cyan touch display on the east glass switches Chinese/English. It uses brightness feedback without mechanical travel. Short presses activate it; camera drags do not.

Six framed specimen tree wells use small exposed soil apertures within stone borders. Layered Terminalia-inspired trees and white frangipani-inspired flowering trees supplement the existing larger garden trees; large roots have wider contained wells. The surrounding garden trays are green groundcover/paving rather than large bare-earth islands. Botanical shape references: [NParks Terminalia mantaly Tricolor](https://www.nparks.gov.sg/florafaunaweb/flora/2/7/2763) and [NParks Plumeria obtusa](https://www.nparks.gov.sg/florafaunaweb/flora/3/0/3072). These are original procedural approximations, not scanned botanical specimens or root-zone engineering designs.

Ten facility entrances have large, related mathematical knot sculptures in matte bronze on dark pedestals, offset from door openings and stairs. The lounge example is retained; the hall, discussion room, library, two residences, tea service, tea pavilion arrival gate and coffee cabin get companion pieces, with geometry shared among four knot types.

Only the arrival-gate logo says “数学难民营”. Individual door signs identify 报告厅, 学术客厅, 讨论室, 图书馆, 研究员居所, 茶室, 咖啡小屋 and 竹庭茶亭.

## Rendering and environment

- Rasterized PBR wood/stone surfaces, physical glass, procedural cloth/metal microdetail, PMREM sky lighting, cached shadows, ACES tone mapping and no bloom. Blackboard surfaces have zero emission, zero specular intensity and a muted chalk tint; button labels also use lit matte materials. Ocean glitter, glass specular and metal gloss are reduced. Rounded furniture retains world-sized bevels. There is no path tracing, displacement tessellation or photogrammetric building reconstruction.
- The pool's planar reflection camera is removed. The only water surface is the 8,000-unit analytic ocean, using the existing normal texture and subdued sun glitter; no new water framebuffer is allocated.
- Four lightweight, irregular green/rocky island meshes occupy distant oblique views; there is still no mainland or mountain mesh beneath the facilities. An unobstructed due-east cone remains for sunrise. The islands use original radial geometry and vertex colors, no downloaded imagery; atmospheric fog provides distance separation.
- Instanced branching trees, folded leaves, palms, ferns, grasses and irregular rock outcrops remain. Three framed giant trees, specimen trees, bamboo groves, lawns and flower borders remain on the garden platforms; the distant natural forest is removed. Contained garden vegetation excludes buildings and seawater; campus planting uses separate supported garden/planter surfaces above the seabed. Leaf breeze is disabled for reduced motion; cached shadows do not track each moving leaf.
- The old natural waterfall/creek model and its source data remain in the repository for possible reuse, but are not imported or created in the live scene.
- Geometry assembly currently counts **546,600 mesh triangles, 3,848 instances and 228 root scene objects**, below the prior 1,285,708-triangle scene and the 1.3M code budget. This is a mesh-count reduction, **not measured FPS improvement**. Spatial instancing bounds draw batches; distant plants do not cast shadows. Site-distance foliage detail is not camera-adaptive LoD.
- No new photographic textures were downloaded. The six 1K landscape JPGs still total 5,921,427 bytes, roughly 32 MiB uncompressed RGBA with mipmaps. Mobile density/MSAA budgets and the independent vector chalk reader remain unchanged. GPU frame rate/memory and visual quality have not been measured in this iteration.
- Fullscreen, hideable controls, tour/light/sensitivity settings and reduced-motion support remain. Mouse rotation defaults to 22% of the original OrbitControls gain; single-touch uses 16.5%. Manual camera ownership is unchanged. No autoplay audio.
- The single arrival logo reads “数学难民营”. Separate NS and Hodge blackboards remain in the academic living villa, including the fractured translucent NS background. They state the mathematical questions rather than announcing solutions. Formulations: [Clay NS statement](https://www.claymath.org/wp-content/uploads/2022/06/navierstokes.pdf), [Clay Hodge conjecture](https://www.claymath.org/millennium/hodge-conjecture/).

## Six-board spectral-sequence classroom

The auditorium has three independent pairs of counter-moving boards, six surfaces total, each with its own canvas texture. Two depth-separated rails let the boards pass while exchanging upper/lower positions. The sequence writes across the three lower boards, lowers the other boards in turn and preserves the previous work above. Grain stays deterministic rather than flickering. Row-wise reveal preserves real SVG mathematical layout; it is a chalk-writing effect, **not reconstructed human handwriting strokes**.

The chalk tip samples the composed page's actual ink columns, excludes empty trailing columns from its motion, and lifts across gaps/row changes. The texture reveal follows the same ink-only progression. If pixel readback is unavailable it falls back to measured text-row bounds, not the old fixed 1,380-pixel sweep. This remains an ink-guided reveal, not linguistically reconstructed stroke order. The shaft wears from 17 cm down to 4.5 cm while the contact tip remains anchored; short pieces are replaced between pages. At most 64 subtle falling-powder particles are recycled. A tilted felt-faced eraser follows short horizontal runs of occupied 32 × 28 pixel ink tiles, lifting across empty areas. Write and wipe duration are proportional to the measured path cost instead of assigning equal time to every row; short letters or equals signs no longer consume a whole row's time. A single incremental wipe cache avoids replaying all past clipping operations every frame. Recently wiped regions darken slightly and have a local low-resolution roughness-map change, then dry out over 12 seconds. Pause freezes wear/dust/drying; reduced-motion hides tools/powder and clears wet trails. Only changing boards upload color/roughness textures. The six 384 × 160 roughness maps add about 1.9 MiB including mipmaps. These are visual effects rather than simulated chalk adhesion or moisture physics.

`build-chalk-notes.mjs` reads the existing `study/spectral` exports and converts their formulas into 37 local SVG pages (one opening title board and 36 teaching units) using build-only MathJax 3.2.2. Pages cover double complexes, filtrations, convergence, Hodge and Leray; the manifest retains the source TeX and section. They reproduce the exported main formula content with short context, not every proof-dialog derivation or interactive diagram. The classroom links back to the original notebook for full statements, proofs and interactions. No runtime CDN is needed. SVG formula paths avoid depending on installed mathematical fonts; Runtime Chinese captions use locally bundled Ma Shan Zheng; mathematical runs use a local print face, including Unicode superscripts and inline variables, with complete Chinese/English caption translations for all 36 pages. Language changes rebuild visible pages and ink guides without changing formula TeX or the current page/lift positions. The vector reader follows the same language. Long explanations retain their full text in the reader; physical boards use up to three lines with an ellipsis. See [MathJax server integration](https://docs.mathjax.org/en/v3.2/server/direct.html).

Controls: select any page, previous/next, pause/resume writing, erase/rewrite, focus the lesson camera, toggle the sea-facing blackout shade, and slide or exchange each board pair. Manual lifting pauses writing. Reduced-motion mode displays complete static pages; page controls and direct lifts still work. Hidden tabs do not advance the lesson. Loading failures appear in the classroom status.

### Mobile clarity and rendering budget

Phones no longer automatically select the old 1×, no-MSAA preset. Fine quality allows native pixel density up to 3×, bounded by a 3.5-million-pixel small-screen budget (6 million on larger screens). A 390 × 844 CSS-pixel, 3× device stays at 3× in either orientation. On compact screens with a multisampled native framebuffer, the scene renders directly with native antialiasing and ACES, bypassing bloom/composer passes. If native multisampling is unavailable, the compositor uses hardware-limited MSAA. Flow mode remains an explicit choice; no device frame-rate guarantee is implied. Resize/orientation changes re-evaluate the budget and dispose outdated MSAA attachments. Compact shadow maps use 1024²; readable pixels take priority over bloom.

Portrait teaching frames the active board rather than retreating to fit the full pair. Board headings/body text use 44/36 texture pixels, short formulas have a consistent scale cap, grain is less intrusive, and mipmaps reduce minification shimmer. Only actually changed board textures upload. The optional **高清板书** layer presents local standalone SVG equations and reflowed HTML explanations, independent of the WebGL texture and postprocessing. It opens on small-screen entry to the teaching chapter; closing it is respected for that chapter. Formula size is adjustable (18–36 CSS pixels); long formulas scroll horizontally rather than shrinking illegibly. It shows a complete page while the 3D classroom retains its writing/erasing animation. Full notebook text is retained in this reader even when the physical board uses a shorter text excerpt.

To regenerate with the pinned extracted `mathjax-full` package: `node visuals/math-refuge/build-chalk-notes.mjs /path/to/mathjax-full-3.2.2`. MathJax's license is included under `assets/chalk/`.

### Contemporary techniques considered

Primary documentation reviewed on 2026-09-19:

- [Three.js WebGPURenderer](https://threejs.org/docs/pages/WebGPURenderer.html): modern WebGPU backend with WebGL2 fallback. Not enabled here: this project currently shares pinned Three.js 0.180.0 WebGL addons and planar-reflection materials. Calling this module WebGPU-rendered would be incorrect.
- [Spark Gaussian splat LoD](https://sparkjs.dev/docs/lod-getting-started/): streamed/adaptive splat rendering can support detailed captured environments. No training-ready, geometrically consistent capture or splat asset was supplied, so Gaussian splatting is not used or simulated by ordinary points.
- [three-gpu-pathtracer](https://github.com/gkjohnson/three-gpu-pathtracer): progressive physically based path tracing. Not included in this version: convergence during continuous travel and target-device performance require a separate validation pass. Current lighting is rasterized PBR with planar reflection, not full path tracing/global illumination.

The requested goal “quality and detail no lower than the video” remains **unverified and not claimed achieved**. Matching every reference room and close-up would require more asset detail and visual comparison. Earlier iterations used code checks only; the September 22 teaching and lighting update also receives browser checks.

## Assets and licenses

- [Ma Shan Zheng](https://github.com/google/fonts/tree/main/ofl/mashanzheng) and [Caveat](https://github.com/google/fonts/tree/main/ofl/caveat), SIL OFL 1.1; local license files accompany both. The Chinese font is subset to all current board captions; build-chalk-font.py verifies glyph coverage and records the font hash. Runtime rendering needs no font CDN.

- Three.js 0.180.0 addons downloaded from the official package distribution on jsDelivr, MIT. Existing core/OrbitControls and the MIT license are reused from `../3d/vendor/THREE-LICENSE.txt`.
- Water normal texture: [Three.js r180 example asset](https://github.com/mrdoob/three.js/blob/r180/examples/textures/waternormals.jpg).
- [Poly Haven wood_floor_deck](https://polyhaven.com/a/wood_floor_deck) and [marble_01](https://polyhaven.com/a/marble_01): 1K diffuse, OpenGL normal and roughness JPGs, [CC0](https://polyhaven.com/license). Served locally; no user images leave the device.
- Landscape: [pine_bark](https://polyhaven.com/a/pine_bark), [forest_ground_06](https://polyhaven.com/a/forest_ground_06) and [rock_boulder_dry](https://polyhaven.com/a/rock_boulder_dry), also CC0, with two 1K JPG maps each. `assets/landscape-sources.json` records the exact upstream download URLs, sizes and SHA-256 hashes. `fetch-landscape-assets.mjs` retrieves them from the official metadata API. Pine bark is a generic weathered-bark surface on the original broadleaf/palm geometry, not a botanical identification or claim of scanned olive/palm bark.
- Geometry, layout and camera choreography are original to this module. Reference author credited for visual direction, with no claim of endorsement.

`fetch-assets.mjs` reproducibly retrieves pinned library dependencies and these public materials. No build step; serve the repository root over HTTP and open `/visuals/math-refuge/`. All runtime assets are local.

## Code verification

### Manual camera ownership

Canvas pointer-down takes over the camera in the document capture phase, before OrbitControls processes the gesture. Dragging, multi-touch, pointer cancellation and window blur cannot restart the tour. Drag-generated compatibility clicks on chapter/tour/board-focus controls are suppressed; a fresh button press is accepted immediately. Light/sensitivity sliders are not treated as camera drags. Orbit auto-rotation is explicitly disabled. Chapter buttons select viewpoints without implicitly starting a tour. The teaching chapter tracks the current board after the configured manual-view idle delay. Resuming snapshots camera position, target and field of view, clears residual OrbitControls damping and eases continuously back over 4.4–13 seconds. No blackout mask or midpoint teleport is used, and transitions use a quintic easing curve with zero endpoint velocity and acceleration. After manual takeover, only the tour button (or its deliberate Space-key shortcut) starts touring; held-key repeats are ignored. On initial load, a broad overview remains visible for five seconds, then the camera eases into chapter 1, the chalkboard lesson, and stays there following the active board. Mode labels and canvas diagnostics update immediately on takeover.

Run `node --test visuals/math-refuge/checks.test.mjs`: **34 code checks** cover ink-gap/trailing-space motion, all bilingual captions, local-time synchronization and preview, fixed two-storey hall, coffee machine, three bridges and clear daytime sky, absence of all pool geometry, shortened dry terraces, stair dimensions and sea landings, distant-island placement and center continuity, offshore foundations and an unobstructed eastern horizon, board orientation/ceiling fit, all eight camera chapters, local assets, finite model buffers and the 1.3M-triangle budget. They also cover three ten-seat rows and darker rising carpet levels, 20% board enlargement/clearance, headrest nonintersection and alignment, all room fixture types and night-time task lighting, physical-button click/drag/cancel behavior, matte non-emissive boards, nonoverlapping platform unions, absence of mainland/exposed piles, dark seat/roof/terrace palettes, ten dry-pad entrance sculptures, above-water camera bounds, retained unused creek data, contained planting, texture hashes, shader replacement hooks, 36 consolidated SVG pages, mobile pixel budgets, chalk/eraser motion, six-board controls and manual-camera ownership.

Scene assembly uses real Three.js geometry with mocked texture/PMREM/canvas/image services. The tests do not compile GPU shaders, render screenshots, measure frame rate or certify architectural safety. The September 22 update is also checked in the browser for night lighting and board interactions. The reference informed the design but was not used for a full-film, side-by-side acceptance test.


## September 22: teaching, local time and smart glass

- Teaching follows the active moving board independently of the tour. Manual camera movement temporarily takes priority; after 20 idle seconds it eases back. The settings slider accepts 5–60 seconds. Holding or cancelling a drag does not unexpectedly restart a tour.
- 36 mathematical units replace 65 isolated-formula pages. `chalk-outline.mjs` records the lecture section references, grouped formulas and distinct bilingual explanations. `chalk-layout.mjs` separates displayed formula rows without splitting fractions; each row finishes before the chalk moves to the next. The repeated byline and artificial “Foundations” numbering have been removed.
- The clock samples the device’s local wall time, including after suspended tabs and midnight. The time slider explicitly enters a frozen preview; the return button restores local-time synchronization. Reduced motion does not stop the real clock.
- Four low-iron smart glass panels replace dense east-wall mullions. Three 8 mm joints and restrained perimeter frames preserve the sea view. The language touch surface is transparent and sits on this glazing, with a quiet cyan selected-language indicator.
- Sculpted bronze/opal pendants, reading bars, soft coves and three separate blackboard wall-wash sources provide layered night illumination. Upstairs rooms now have actual lights as well as visible fixtures. These are artistic real-time lighting settings, not a calibrated illuminance simulation.

- All mathematical expressions, including inline variables, Unicode superscripts and symbols in titles or prose, use print faces in the 3D board, vector reader and generated SVG pages. Chinese prose retains the chalk font.
- Controls are unframed line icons in the upper-right corner, with tooltips and accessible names. Chapter navigation uses a fine active underline.

- Four finite-range warm fill lights remain. Each board column now has a centered `中  Eng` touch label, with no arrows or underline. The three controls share language state. Smart-screen text uses the spectral cover's Chinese calligraphy, old-style Latin serif and silver gradient; hover applies a single text-masked sheen rather than lighting the whole panel.

- Six relevant teaching units now include compact vector diagrams from the notebook conventions: a double complex, total-degree diagonal, column filtration, second-page differential, zeroth page and stable-page diagonal. Formula and diagram ink regions are disjoint; labels remain print mathematics.
- Writing speed is adjustable from 0.25× to 2× independently of touring. Chinese ink contact advances at half the mathematical-ink speed; the chalk shaft tilts down and outward from its anchored upper tip.
- The hall has a bounded diffuse-irradiance fill approximation, in addition to actual lamps, to keep the ceiling, upper boards and chair backs from falling into black. It is an artistic approximation to bounced light, not ray-traced global illumination.
- Continuous thin edge strips follow the union boundaries of platforms and roofs, with room perimeter coves, bridge edges and step nosings. Twenty-six platform lanterns alternate stone niches, bronze bollards and slender poles. Their soft ground pools are shared transparent falloff patches; they do not each add a realtime shadow map.

- Sky scattering and water shading now follow the same day/night factor as the scene clock, so midnight no longer leaves a white horizon and daytime-blue sea behind the room lighting.

- Board teaching is chapter 1. The opening overview holds for five seconds after loading, then moves continuously to that teaching view. Chapter buttons, board-focus actions, tour resumes and automatic tour boundaries all share the same position/target/FOV transition; manual dragging can interrupt any transition.

- Camera transitions now run at half the previous speed (4.4–13 seconds according to travel distance); the initial overview hold remains five seconds. Board-follow camera damping is also slower.

- The opening board presents the lecture title, author 孟晟 (Sheng Meng in English) and scope, followed by the 36 teaching units. Non-mathematical English prose, titles and author names use the local Caveat handwriting face; embedded mathematical variables and formulas retain print typography. The title board has a longer reading pause.

- Exterior stairs have tread-mounted posts, baseplates, continuous landing returns and steel stringers. The hall flight meets its landing without burying the final treads. Sea-access handrails have matched post/rail anchors.
- Each board column has white, yellow, pink and blue chalk, plus a felt-backed eraser that lifts from its own tray before wiping and returns smoothly afterward. Ink removal waits until the felt reaches the board; pause freezes pickup and return.

- Camera travel uses quintic position interpolation with incoming velocity/acceleration, fixed destination framing and quaternion spherical interpolation. Native desktop MSAA avoids an unnecessary HDR compositor/output pass; texture uploads are warmed behind the loader. DOM status updates run at 10 Hz while camera motion stays on the animation frame loop. Bounded frame/CPU samples are exposed on the canvas dataset for local performance checks.

## Background music

The user-provided “FLYING OVER NORWAY (4K UHD) 1HR Ambient Drone Film + Music by Nature Relaxation for Stress Relie.mp3” is included as `assets/audio/norway-ambient.m4a`: the full 57:27.513 stereo recording, AAC at 96 kb/s with fast-start metadata, a two-second entrance fade and three-second ending fade. The original file is unchanged. The web copy is approximately 42.1 MB and is requested only by the post-loading “点击进入” button or the music button; it does not block the 3D scene loader. The entrance holds the opening camera and lecture clock until the user enters. Playback loops at 18% initial volume, with remembered pause/volume preferences, gentle volume transitions and suspension while the tab is hidden. The recording is user-supplied and is not covered by the CC0 texture asset licenses.

`node --test visuals/math-refuge/music.test.mjs` checks on-demand playback, autoplay rejection/retry, rapid toggle races, volume persistence, hidden-tab suspension and disposal.

## 2026-09-23 — Blackboard hardware and connected hall

- Every sliding board has an opaque 80 mm local-unit-thickness body, a solid rear face and rear stiffeners; the writing face remains matte and non-emissive. The nanmu-style frame is half the former width, with deterministic fine golden wood-grain color/bump textures. This is a procedural material interpretation, not a scanned rare-wood sample.
- Each column has two full-height metal guide channels, wall mounts and four moving guide rollers per board. The two depth tracks still clear one another during a full lift exchange.
- Three wider nanmu trays contain the four colored chalks and a rounded eraser with raised palm grip, recessed finger grooves and textured laminated felt. The felt rests on the tray floor, with the whole eraser ahead of the moving frame and inside the tray lips. Its existing pickup/return and ink-only wipe animation is retained.
- The report list moves 0.55 local units left. Its heading is the current Shanghai-calendar date, refreshed across midnight. Only the selected speaker has a leading pointer; no selection underline remains. Hover sheen also respects reduced motion.
- The seamless eastern smart wall retains the open sea view. West, north and south automatic doors use stationary hover sensors, two independently sliding leaves and a 1.2-second close delay, preventing motion-induced hover flicker. The west side has four fixed panes around its entrance; each lateral side has two. Fixed transoms close the space above each door.
- The user selected generated lighting sketch **A: floating oval**. One low-suspended bronze oval with a warm shielded lower diffuser replaces the previous ceiling strips. It is over the audience zone with more than 2 plan units of clearance to the separate board-wash fittings. Sketches B/C remain unbuilt; no other room details from generated sketches were adopted.
- Rounded tube junctions close the post/rail joints. Continuous guards follow the union of the three occupied upper-floor terraces and landings, leaving only specified stair openings. The hall flight and landing shift inward by 1.65 plan units, preserving 34 treads and their slope; the first tread ends before the south facade/sign plane.
- The hall's entrance sculpture and pedestal are removed. The main terrace is centered at the hall center (39, 0), retaining its 30-by-33 plan size. The approach bridge, canopy and rear door now share Z=0; the canopy reaches the door plane. The small north bridge is adjusted to the new platform gap. Seats, report contents and building orientation do not move.

Code verification: 44 tests across `checks.test.mjs`, `music.test.mjs` and `report-loader.test.mjs`, including solid rear-face ray hits, eraser/tray clearance, centered controls, hover state, delayed doors, terrace perimeter openings, pendant clearance and a stair foot inside the facade. No browser visual acceptance was performed.

## 2026-09-23 — Selected upstairs concept A and interactive lectern

- The user selected the sea-view academic lounge composition: six differently colored iMac-style workstations in two west-side trios; three east-side circular tables with open C-shaped upholstered sofas; north-wall tea, coffee and drinks bar. The room envelope, west stair arrival and fixed roof remain unchanged. There are sixty modeled beverage bottles, a twin-head espresso machine, grinder, tea kettle, sink, cups and three bar stools. Low planting consoles and subdued wood/textiles separate circulation from discussion. Repeated furniture, keyboard keys and bottles use instanced batches; screens use local canvas textures, without downloads or continuous redraws.
- A ninth camera chapter, “二楼客厅”, travels inside the upper lounge at standing eye height. Existing chapter indices are unchanged.
- The ground-floor lectern has a matte graphite chassis, bronze trim, sloped embedded display, USB-C recesses, stylus, supported console and gooseneck microphone with modeled grille. The display directly operates previous/next page and writing pause/resume. It does not request microphone permission or record audio.
- Clicking the lectern body transitions from the current camera pose to an eye point 1.68 metres above its floor, behind the lectern and facing the audience. Manual orbit and movement remain available. Automatic board following is suppressed until another camera chapter or the explicit resume-tour button is selected. Drag/cancel gestures do not activate a control; touch state redraw is capped by change detection.
- Verification: 46 code/geometry/interaction tests across `checks.test.mjs`, `lectern.test.mjs`, `music.test.mjs` and `report-loader.test.mjs`. Includes room-envelope bounds, six distinct workstation colors, separated discussion bays, standing camera height/direction, touch ray hits, cancelled drags and manual camera ownership. No browser visual acceptance was performed, in accordance with the user's code-check-only preference.

## 2026-09-23 — Flush-mounted boards and complete console framing

- Restored the original `#735c3e` warm-brown hardware tone and 0.65 roughness, retaining subtle neutral wood grain. The chalk tray depth is now 0.40 local units, between the original 0.23 and the oversized 0.56; erasers remain wholly within its lips and clear the moving frames.
- Removed the common opaque backing wall, not the six boards' own solid backs. The board/track/tray/tool assembly shifts 0.595 local units toward the smart glass. The metal mounting pads now stop less than 5 mm inside its inner face, retaining the paired guide tracks and avoiding coplanar flicker. Smart-glass labels remain on the glass instead of shifting into it.
- Each language toggle shows only its current state: `中` or `Eng`, centered below its board column.
- Speaker eye height remains 1.68 m. The camera stands 1.35 m behind the lectern and pitches toward the console and audience. Aspect-aware FOV fits all display corners with a 16% edge margin, checked at desktop, tablet and 320/932–390/844 portrait ratios; resizing updates the FOV without changing position. The microphone is modeled hardware, not an audio recording feature.
- All 46 code/geometry/interaction tests pass. Browser visual QA was not performed.

## 2026-09-23 — Full-height glass doors

All three modeled automatic glass entrances now match the hall's 4.9 m glass-wall height. The three upper transom panes and their intermediate horizontal frames are removed; sliding tracks sit above the full-height opening at ceiling level. Handles remain at 1.1 m above the floor instead of rising with the taller leaves. Hover opening, delayed closure and the fixed side-pane counts are unchanged. Geometry tests verify door-bottom/top alignment, reachable handle height and overhead track clearance; the 46-test suite passes.

## 2026-09-23 — Clear discussion openings and revised iMac workstations

- Removed the three single chairs in the C-sofa openings. Removed plants from round tables and the low central consoles; only the two floor-standing corner planters remain. Table books, three sofas and round tables are retained.
- All six desks use one warm walnut-brown finish, and all work chairs share the same muted gray-green upholstery. Only the computers retain their six distinct colorways; no alternating colored desktops remain.
- Rebuilt the stylized computer bodies using Apple's current iMac front/side/rear references and published 54.7 cm width / 46.1 cm overall height. Thin rounded cases, off-white screen surround, pale color-matched chin, separate rear hinge/support, flat base, camera and rear ports replace the previous overlapping boxes. The display uses a 16:9 local canvas; no Apple photography or software functionality is bundled. This is a procedural visual approximation, not Apple CAD.
- The old stand reached forward through the display plane. Every new stand component is behind the case; 150 perspective-ray samples across all six display surfaces now hit the display first. Additional rays verify that the former visitor-chair footprints are clear to the rug. All 46 tests pass; no browser visual QA was performed.
- Official references checked: https://www.apple.com/imac/ and https://www.apple.com/imac/specs/ (front, side and rear images linked from the product page).

- Smart-glass hit planes now have color writes disabled permanently, including hover/press states; only alpha-masked lettering provides feedback. Language buttons show the **destination** language: Chinese content displays `Eng`, and English content displays `中`. This supersedes the earlier current-state label behavior.

## 2026-09-23 — Centered upper entrance without landing columns

The hall's upper landing is now centered at Z=0, matching the west lounge entrance. The two tall columns beneath it are removed; shallow horizontal beams meet the upper slab, while the stair stringers and continuous guards remain. The 34-step flight keeps its lower end at Z=9.6 and uses approximately 35.5 cm physical treads, so the upper arrival is aligned without projecting the foot past the south facade. Geometry checks cover center alignment, connected tread/landing/rails, tread depth, the clear lower route and absence of the former tall columns. This is scene modeling, not a structural-engineering certification.

## 2026-09-23 — Ring sofas match the auditorium seats

All three upstairs ring sofas now reuse the auditorium's actual materials: warm-brown `#776352` fabric with the same normal/roughness maps, dark `#544e45` lower shell and matching brass feet. All fifteen throw pillows use that same warm-brown fabric; the green/coral/purple upholstery and contrasting pillows are removed. Shapes, layout, computer colors, bar stools and other furniture are unchanged. Shared materials remain owned and disposed by the campus. Regression checks verify material identity across floors, fabric textures and all fifteen pillows; the 46-test suite passes without browser visual QA.

The six redundant pale grab handles beneath the sliding boards are removed at the user's request. Board frames, guide rollers, tracks, trays and lift controls are retained.

## 2026-09-23 — Tighter vertical blackboard spacing

Board dimensions stay unchanged. Resting centers are now 1.835 and 3.960 in lecture-local coordinates: the physical frame-to-frame seam is 2.88 cm and the upper frame clears the top rail stop by 4.86 cm. Trays, lips, chalk and parked erasers rise together by 23.04 cm, leaving 4.57 cm below the lower frame. Lower tracks and mounting pads are shortened upward, and paired-board camera focus follows the new center. Geometry regression checks sample 101 lift positions per column, covering separate depth channels, rail-stop clearance and the three resting seams. All 46 code tests pass; no browser visual QA was performed.

## Three-storey reading seminar and room controls

Based on the GitHub mainline through `ac3fefb`, preserving its lectern, automatic doors, upper lounge, paving, board hardware and local-definition chalk annotations. The independent west building has three smaller classrooms, each with two rows of six seats, six sliding boards, an interactive lectern, automatic glass entrance, lighting and independent report state. Supported exterior switchback stairs connect all floors.

The first classroom follows Kollár–Mori (1998), 7 chapters / 38 sections / 336 boards. The left navigator offers chapter and section choices and a direct link to the Lemma 5.17(2) erratum computation; each section is an independent student session ending without looping. See `assets/chalk/km/SOURCES.md` for attribution, proof-status conventions and the counterexample. The upper classrooms use the existing report catalogue independently.

Board-related controls occupy a frameless right-side dock only while the camera is inside a teaching room. The camera's position determines which classroom is controlled, including when entering manually. Outdoor door sensors remain active. The original upstairs lounge is still camera 09; the discussion building is camera 10.

Lectern displays show the current board number and total. Previous/next, the screen progress bar and the accessible page/slider controls seek atomically: load the last six relevant pages, rebuild their slots, set board heights immediately, reset chalk/eraser/dust and show complete ink without lifting, erasing or writing transitions. Rapid seeks keep only the latest result. Explicit playback resumes normal writing; section/final endings remain stopped.

### Section sessions and render scheduling

The physical glass left of the KM boards now supports chapter folders and clickable section choices. A chapter is not a session. All seminar rooms initially have blank boards and require an explicit topic selection; entering, pausing or changing floors never starts a talk. Each section has its own title, closing and local progress count. The latest edition has 274 pages across 38 independent section sessions. Related sparse boards are grouped into authored cue/formula blocks; each section additionally has a worked argument with its precise scope and theorem inputs recorded. These include special cases and local calculations, not complete proofs of all the general theorems in KM98.

Every 100 ms, room visibility checks distance with hysteresis, camera frustum and storey occlusion. Hidden rooms do not redraw chalk canvases, update textures, animate tools or update off-screen lectern canvases. Lecterns have their own visibility bounds, so the speaker console remains usable when the blackboards are behind the camera. Started sessions advance a lightweight clock using estimated write durations; unselected and paused sessions stay still. Reentry loads only current on-board pages, spread across frames, and restores partial progress without restarting the session. These estimates intentionally do not reproduce every offscreen chalk stroke. Blackboard hardware and its last painted contents remain visible at all distances, including during reentry loading; only live chalk canvas updates, texture uploads and tool animation are suspended. Static hardware transforms are cached. Shared architectural instances are split by building/storey. Architecture is never distance-hidden: stair treads, handrails, landings and structural boxes retain their geometry. Rounded furniture alone switches to a lower-tessellation model below a conservative screen-space threshold (6/8 px hysteresis), with identical outer bounds.

Seminar body boards omit repeated section numbers and headings. Opening title boards and closing thanks boards remain; navigation and the accessible reader retain identifying metadata. The three classrooms opt into this presentation independently of the main auditorium.


### September 23: preserve architecture and write mathematical arguments

- Removed the size-and-distance visibility heuristic that accidentally hid stair treads. Screen-space rounded-corner LOD reduces the measured instanced architecture from 742,018 to 607,810 triangles in the far-camera fixture (18.1%); every batch stays visible and structural geometry is identical. This is a geometry count, not an FPS guarantee.
- Optional automatic resolution uses asynchronous `EXT_disjoint_timer_query_webgl2` results, not browser frame scheduling. Sustained GPU pressure adjusts scale in 4% steps, with a 76% floor outdoors and 96% while reading; recovery is slower. Fixed quality is available. Missing or invalid GPU timers retain the selected resolution. Existing cached sky reflections and shadows remain cached.
- Board flow interleaves short bilingual cues and separate equation rows. Related sparse pages can share one board; diagrams and locally annotated definitions retain their authored grouping. Prose explanations remain in the high-resolution reader. The four main talks retain at least 24 substantive boards each.
- KM includes 38 new worked arguments (152 individual cue/equation steps), covering each of its 38 sections. Examples: pointed deformation counts, weighted slopes, negative curves, blow-up Jacobians, Kodaira via the Bochner identity, curve basepoint-freeness, ADE intersection matrices, Tjurina quotients, and explicit small-resolution charts. The existing Lemma 5.17(2) counterexample remains intact.
- Proof coverage is deliberately scoped: curve cases do not prove the general basepoint-free/nonvanishing theorems, local flop charts do not prove general flip existence, and section extension does not by itself prove finite generation. These major general proofs still need development. Each worked page records what it actually establishes and its external inputs.
- Browser QA: Chinese/English proof canvases, far-view retained steps, no WebGL console errors, GPU timing supported in the tested browser. Automated checks cover all board reveal rectangles, font coverage, section selection, offscreen scheduling and LOD invariants.


## September 24: buildings, board layout and retained lecterns

The bottom navigation lists nine numbered buildings, with matching translucent roof numbers. The auditorium includes its upstairs lounge. A background-free left list offers the rooms in the selected building; blackboard controls and KM chapter/section navigation are on the right.

Physical boards restore concise mathematical explanations. Normal body pages target six to eight measured lines spread over the writing area, retaining all equations and bilingual text; diagrams and locally annotated definitions retain their authored grouping. Auditorium body boards retain section headings; seminar body boards omit repeated headings.

The second and third teaching-building classrooms temporarily have blank boards, without loading reports or accepting touchscreen interaction. All lectern furniture remains visible. Screen display, touch targets and canvas updates can be suspended independently of the furniture.

Reentering during an erase phase now restores the cached erase plan even when the wet-stroke cache has been cleared. The regression covers leaving mid-erase, returning at the same progress and continuing into writing. Duan's Chinese display name is 段治豪, with the font subset extended accordingly.

Hu’s report has been rebuilt from Du–Hu, arXiv:2606.31170v1: 30 substantive boards plus opening and closing pages. Authored bilingual lines interleave explanations and equations; source correspondence and the scope of external inputs are recorded in `assets/chalk/hu/SOURCES.md`. English prose and section numbers use the user-selected Marck Script. Mathematical formulas retain their existing font while alternative mathematical typography is reviewed.

Handwritten text now measures actual glyph ascents, descents and horizontal overhangs. Line layout and reveal clips share those metrics, preserving Marck Script descenders and inline subscripts instead of assuming a fixed bottom margin.

### Mobile Safari resource budget (v79)

Based on upstream `253a1e4` (including the residence, volumetric clouds and shallow-water shelves). iPhone/iPad detection includes desktop-mode iPad Safari and remains stable on rotation. Mobile rendering caps the high-quality framebuffer at 1.2 million pixels / 2× DPR, avoids native MSAA and additional HDR compositor buffers, and debounces backing-store resizes. Desktop display limits remain unchanged.

Startup now renders the visible scene without compiling, uploading and force-drawing the entire campus. Context loss is handled before asynchronous loading begins; network/image/font work has deadlines, and an independent 60-second watchdog provides retry controls. The error screen offers `?safe=1`, which uses analytic sky/cloud fallbacks and disables shadow maps. It preserves buildings, steps, blackboards and course content.

Unselected/disabled rooms use shared 128×64 blank board textures. Written mobile boards allocate 768×320 textures on demand (one quarter of the original area), while authored stroke coordinates and the vector reading view retain their original precision. Offscreen boards retain the last written texture. Canvas caches explicitly release evicted backing stores. Mobile cloud targets are 256×128 with 16 steps, a 32³ density volume and 500 ms refreshes; atmosphere targets are 128×64 with 1-second refreshes. Cloud frames blend continuously. Neutral reflection probes no longer build duplicate volumetric resources.

Validation: 85 automated checks passed, including classroom writing/erasing/seeking, lazy board allocation, iPad/iPhone budgets, resource deadlines, sky render-target fallback and refresh throttling. Desktop Safari entered both ordinary and safe modes. Chromium at a 390×844 viewport entered safe mode, continued multiple board pages and emitted no console errors. These are desktop browser checks, not measurements on a physical iPhone; actual affected iPhone/iOS remains unverified.

### Performance panel (v80)

The unframed gauge icon beside Settings opens a live panel with FPS, a rolling trend, average/P95 frame time, P95 in-frame CPU work, asynchronously sampled GPU scene time, draw calls, triangles, texture/geometry counts, drawing-buffer resolution, pixel ratio and active classroom count. FPS includes long frames and is computed from elapsed frame intervals, not averaged reciprocal times. Statistics update at 2 Hz from the existing animation loop; closing the panel stops its sampling and DOM updates. Backgrounding clears the sample window. Unsupported GPU timing is labelled explicitly, and resource counts are not presented as VRAM estimates. Desktop and 390×844 browser layouts, close behavior and console were checked; all 87 automated checks passed.

### iMac GPU optimization (v81)

Profiled on the local Apple M4 iMac (10 GPU cores, 32 GB), in the Codex Chromium browser. The old settled board view used a 2457×1382 backing buffer with 4× native MSAA: frame P50/P95 were 49.1/66.3 ms and CPU P95 was 7.1 ms. This points to graphics work rather than JavaScript as the principal bottleneck in that sample.

The material hook now rejects finite-range point/spot lights outside their original radius and skips the direct BRDF when the original light contribution is zero. It does not remove lights, shorten their range, reduce their intensity, change shadow maps, or hide any geometry. Three vendor chunks remain untouched; the optimization is composed with the existing room-fill hook and has a separate program-cache key. With this shader change alone, the same camera position, buffer, 289 draw calls and 308,630 triangles measured frame P50/P95 of 16.7/33.6 ms. GPU estimates decreased from 85.6 to 28.6 ms in those snapshots; these are asynchronous rolling samples, not a laboratory benchmark.

Desktop cloud targets now use 512×256, 24 steps and 250 ms blended refreshes. Their nominal raymarch step budget is about 7.5% of the previous 1024×512, 32-step, 100 ms setting; this is not an FPS multiplier. The same density volume, weather, wind motion and optical model remain. GPU queries now include cloud/atmosphere rendering as well as the main scene, so automatic quality no longer ignores that workload.

Desktop high quality caps the backing buffer at 4 million pixels (2.8 million when GPU timing is unavailable). Sustained GPU pressure can reduce the desktop reading scale to 0.80 and outdoor scale to 0.65, with faster overload response and slow recovery. The former 0.96 reading floor prevented meaningful relief. Mobile retains its existing 0.96/0.76 floors. Native desktop MSAA, board textures, vector reader and all architectural geometry remain. In the fully optimized first-column board view, the browser measured P50/P95 of 16.7/17.0 ms with a 2099×1180 buffer; this final snapshot has a different board position from the shader-only A/B and should not be treated as a pixel-identical comparison. Browser, viewport and background applications affect results.

Validation: 90 tests passed, including finite-range light/BRDF guards, indirect room lighting, Retina pixel budgets, sustained-load response, fixed-quality override, mobile reading limits, all existing geometry/board tests. The optimized shader compiled and ran without console errors in the tested browser.

The v81 depth-order follow-up draws opaque architecture before ocean and sky (transparent glass still draws last). This allows early depth testing to reject expensive background shading behind classroom walls and boards. At 23:59 with the same first-column camera and 261 calls / 307,000 triangles, the night GPU rolling estimate fell from 27.7 to 12.5 ms; frame P50 stayed 16.7 ms and P95 remained about 33.3 ms, so occasional slow frames still exist. Screenshots confirmed the night board lighting and geometry remained intact; no console errors occurred. The updated 42 scene/board checks passed, including depth-test/order assertions.

### Frame-time stability (v82)

- Cloud refreshes now render four scissored stripes over four desktop frames (two on mobile), using a frozen weather/camera sample. Only complete panoramas become visible, after the previous blend finishes. Resolution, ray-march steps and the two-target memory budget are unchanged; the initial panorama is still prepared before entry.
- Dry handwriting no longer redraws or uploads the unchanged roughness map. Ink updates and wet eraser trails retain their existing behavior.
- Residence lights keep a fixed shader light count and fade their intensity over distance, avoiding shader variants caused by switching light visibility while travelling.
- Dynamic resolution reacts to sustained GPU times above 15 ms and restores resolution only below 9 ms after a 10-second cooldown, leaving more room for a 16.7 ms frame deadline. Geometry and existing minimum scales remain intact.

Validation: 91 automated tests passed, with focused scene and cloud tests rerun after adding assertions for dry texture uploads, fixed light count, stripe coverage, frozen uniforms, completed-image publication and renderer-state restoration. In the local M4 iMac's Codex Chromium browser at a 1280×720 viewport, night writing measured frame P50/P95 16.7/17.6 ms (scale 0.80, 2048×1152 buffer); the cloudy midday sea terrace measured 16.7/17.5 ms (scale 0.92). The preceding live version also measured near 60 FPS in this session (night P95 17.7 ms at scale 0.95), so these samples establish healthy operation, not a controlled FPS speedup. Cloud and board screenshots showed intact rendering, with no console errors. These are rolling samples, not a guarantee of 60 FPS across hardware, browsers or every camera move.

### Curved eraser motion (v83)

Local wiping passes now follow a shallow upward bow, ease into and out of each pass, and gently rotate the eraser. Lifted travel also curves between occupied regions. The tool, ink removal and damp trails share the same sampled path; tray pickup/return stays intact. All 42 scene/board checks passed, including dense and sparse ink coverage.

### Whole-scene frame feedback (2026-09-25)

The Ocean-only fallback did not cover cloud/shadow cost on Safari without GPU timing, and reading protection held GPU-based resolution at 100%. A second, foreground-frame controller now runs regardless of GPU-query support or whether the performance panel is open. Two-second samples reduce effects after sustained missed deadlines: first standard clouds/shadows and simple water, then cloud/shadow/particle effects off, then 90%, 80%, and 70% rendering scale (75% minimum while reading). Board texture resolution and scene geometry remain unchanged. Recovery requires 20 healthy seconds; a failed recovery waits 60 seconds before another upgrade. Background gaps and isolated stalls are discarded, manual fixed quality is respected, and a 120 FPS preference is bounded by observed browser cadence. This cannot override OS/browser power-saving frame caps.

The performance panel and settings now show the actual adaptive tier and applied cloud/shadow/resolution state. Conservative recommendations no longer treat every non-Intel GPU as high-end or start at 125% supersampling; existing explicit preferences remain saved. Validation: 120 tests passed, including synthetic 30 FPS / fluctuating 20–40 FPS without GPU timings, reading fallback, recovery, background pauses and manual controls. The local M4 Chromium board view automatically reached tier 3 (clouds/shadows off, 90% render scale) and measured about 60 FPS, P95 17.1 ms, with no console errors. This is not a measurement of the user's other MacBook Air or Safari.

### Stored-board rendering audit (2026-09-25)

The fully lowered six-board rig was already invisible to Three.js (including its shadow traversal), with the normal board-update loop returning early. Its retained textures are a reuse cache, not ongoing draw calls. Fixed two gaps: the performance counter used room visibility rather than actual board-update eligibility, and explicit seeking could still repaint a stored board. Stored or travelling boards now defer texture writes, the fully stored update immediately returns, and raising repaints the selected content. Removed the duplicate storage control ID.

Validation: all 42 scene/board checks passed, including 600 fully stored frames without ink/roughness texture-version changes, hidden seeking without board uploads, and restoration after raising. In-browser at the report-board camera, overall draw calls changed from about 281 to 134 after lowering; the active board-room list became empty. These are whole-scene calls, not a per-object GPU timing measurement. No console errors occurred.
